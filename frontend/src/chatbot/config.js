import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "Urban Vista AI",
  initialMessages: [createChatBotMessage(`Hello! I can help you explore the city.`)],
};

export default config;
